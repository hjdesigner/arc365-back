type FirstInteraction {
  id_first_interaction: ID!,
  status_first_interaction: Int!
}

type Query {
  first_interaction: [FirstInteraction]!
}

type Mutation {
  updateStatusFirstInteraction(id_first_interaction: ID!, status_first_interaction: Int!) : FirstInteraction!
}
