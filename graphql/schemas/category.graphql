type Category {
  id_category: ID!,
  name_category: String!
}

type Mutation {
  addCategory(name_category: String!) : Category!
  uploadCategory(id_category: ID!, name_category: String!) : Category!
  deleteCategory(id_category: ID!): ID!
}

type Query {
  categories: [Category!]!,
}
